#lists are ordered and changable 
#lists are mutable which means we vcan change the values in the list
mutable=[10,10,28,49]
print(mutable)
mutable[1]='changed'
print(mutable)
#lists can contain same type variables
print([8,24,76])
#lists can contain strings
print(['red','yellow','blue'])
#lists can contain combination of integers floats and strings 
print(['red',2,8.9])
# lists can contain another lists
print([1,2,[3,4],9,0])
#list can be empty
print([])
